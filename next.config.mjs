/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['your-image-domain.com'], // Add external image domains here if needed
    },
  };

  export default nextConfig;
